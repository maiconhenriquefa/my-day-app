import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderTopWidth: 2,
    width: '100%',
    height: 90,
    backgroundColor: '#000',
  },
});

export default styles;
